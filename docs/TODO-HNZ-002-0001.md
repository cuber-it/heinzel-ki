# TODO: HNZ-002-0001 — Core Datenmodelle

Branch: `mvp-002`  
Zieldatei(en): `src/core/base.py`, `src/core/models.py`, `src/core/exceptions.py`  
Referenz (nur lesen): `~/Workspace/theBrain/code/core/src/heinzel/models/`

---

## Paketstruktur

- [ ] `src/core/__init__.py` anlegen (leer, macht `core` zum Package)
- [ ] `src/core/models.py` anlegen
- [ ] `src/core/exceptions.py` anlegen
- [ ] `test/` Verzeichnis anlegen (falls noch nicht vorhanden)
- [ ] `test/core/` anlegen
- [ ] `test/core/__init__.py` anlegen
- [ ] `test/core/test_models.py` anlegen

---

## exceptions.py

Aus Referenz `models/errors.py` übernehmen und erweitern:

- [ ] `HeinzelError(Exception)` — Basis, mit `message` + `details`, human-readable `__str__`
- [ ] `AddOnError(HeinzelError)` — NEU: `addon_name`, `hook_point`, `original_exception`
- [ ] `AddOnDependencyError(AddOnError)` — NEU: fehlende Abhängigkeit
- [ ] `AddOnLoadError(AddOnError)` — NEU: Fehler beim Laden
- [ ] `CircuitOpenError(AddOnError)` — NEU: `addon_name`, `failure_count`
- [ ] `ProviderError(HeinzelError)` — `status_code`, `detail` (aus Referenz, leicht anpassen)
- [ ] `DatabaseError(HeinzelError)` — `query`, `original_exception` (aus Referenz erweitern)
- [ ] `ConfigError(HeinzelError)` — `missing_key`, `config_path` (aus Referenz erweitern)
- [ ] `SessionError(HeinzelError)` — NEU: `session_id`
- [ ] `StrategyError(HeinzelError)` — NEU: `strategy_name`
- [ ] Alle sind direkt aus `heinzel_core` importierbar (d.h. Re-Export in `__init__.py`)

---

## models.py — Basis-Models (Pydantic, frozen=True)

Aus Referenz direkt übernehmen:

- [ ] `Message` — `role: str`, `content: str | list` (aus `models/chat.py`)
- [ ] `TokenUsage` — `input_tokens`, `output_tokens`, `total` Property (aus `models/chat.py`)
- [ ] `ToolCall` — `call_id: str`, `tool_name: str`, `args: dict` (Felder an Story-Spec anpassen, NICHT von Referenz `id`/`name` übernehmen)
- [ ] `ToolResult` — NEU: `call_id`, `result`, `error`, `duration_ms`

Neue Platzhalter-Models (Details kommen in späteren Epics):

- [ ] `MemoryResult` — `source: str`, `content: str`, `relevance: float (0.0-1.0)`, `metadata: dict`
- [ ] `ThinkingStep` — `iteration: int`, `thought: str`, `action: str`, `observation: str`, `snapshot_id: str`
- [ ] `AddOnResult` — `modified_ctx: 'PipelineContext'`, `halt: bool`, `ack: bool`, `error: str | None`
- [ ] `Fact` — `key: str`, `value: str`, `heinzel_id: str | None` (Platzhalter, Details in HNZ-003)
- [ ] `Skill` — `name: str`, `version: str`, `description: str` (Platzhalter, Details in HNZ-003)
- [ ] `Goal` — `id: str`, `description: str`, `status: str`, `priority: int`, `parent_id: str | None` (Platzhalter, Details in HNZ-011)
- [ ] `ResourceBudget` — Platzhalter: `max_tokens: int`, `max_iterations: int`, `max_tool_calls: int`
- [ ] `StepPlan` — Platzhalter: `steps: tuple[str, ...]`
- [ ] `Reflection` — Platzhalter: `text: str`, `snapshot_id: str`
- [ ] `EvaluationResult` — Platzhalter: `score: float`, `reasoning: str`

---

## models.py — HookPoint Enum

- [ ] `HookPoint(str, enum.Enum)` mit allen 23 Punkten:
  - `ON_INPUT`, `ON_INPUT_PARSED`, `ON_MEMORY_QUERY`, `ON_MEMORY_HIT`, `ON_MEMORY_MISS`
  - `ON_CONTEXT_BUILD`, `ON_CONTEXT_READY`
  - `ON_LLM_REQUEST`, `ON_STREAM_CHUNK`, `ON_THINKING_STEP`, `ON_LLM_RESPONSE`
  - `ON_TOOL_REQUEST`, `ON_TOOL_RESULT`, `ON_TOOL_ERROR`
  - `ON_LOOP_ITERATION`, `ON_LOOP_END`
  - `ON_OUTPUT`, `ON_OUTPUT_SENT`
  - `ON_STORE`, `ON_STORED`
  - `ON_SESSION_START`, `ON_SESSION_END`
  - `ON_ERROR`

---

## models.py — PipelineContext

- [ ] `PipelineContext(BaseModel, frozen=True)` — alle Felder mit sinnvollen Defaults

  **Input-Schicht:**
  - [ ] `raw_input: str = ""`
  - [ ] `parsed_input: str = ""`
  - [ ] `is_command: bool = False`
  - [ ] `user_strategy_hint: str | None = None`

  **Wissens-Schicht:**
  - [ ] `memory_results: tuple[MemoryResult, ...] = ()`
  - [ ] `short_circuit: bool = False`
  - [ ] `messages: tuple[Message, ...] = ()`
  - [ ] `system_prompt: str = ""`
  - [ ] `model: str = ""`
  - [ ] `provider: str = ""`
  - [ ] `facts: tuple[Fact, ...] = ()`
  - [ ] `skills: tuple[Skill, ...] = ()`

  **Zustands-Schicht:**
  - [ ] `loop_iteration: int = 0`
  - [ ] `loop_done: bool = False`
  - [ ] `goals: tuple[Goal, ...] = ()`
  - [ ] `budget: ResourceBudget = Field(default_factory=ResourceBudget)`
  - [ ] `step_plan: StepPlan | None = None`
  - [ ] `reflection: Reflection | None = None`

  **Produktions-Schicht:**
  - [ ] `stream_buffer: str = ""`
  - [ ] `thinking_steps: tuple[ThinkingStep, ...] = ()`
  - [ ] `tool_requests: tuple[ToolCall, ...] = ()`
  - [ ] `tool_results: tuple[ToolResult, ...] = ()`
  - [ ] `evaluation: EvaluationResult | None = None`

  **Output-Schicht:**
  - [ ] `response: str = ""`

  **Meta:**
  - [ ] `session_id: str = ""`
  - [ ] `heinzel_id: str = ""`
  - [ ] `timestamp: datetime = Field(default_factory=datetime.utcnow)`
  - [ ] `phase: HookPoint = HookPoint.ON_INPUT`
  - [ ] `metadata: dict[str, Any] = Field(default_factory=dict)`

  **Navigation:**
  - [ ] `previous: 'PipelineContext | None' = None`
  - [ ] `snapshot_id: str = Field(default_factory=lambda: str(uuid4()))`

- [ ] `model_config = ConfigDict(frozen=True)` (Pydantic v2)

- [ ] `evolve(**changes) -> 'PipelineContext'` Methode:
  - [ ] `previous=self` automatisch setzen
  - [ ] `snapshot_id` neu generieren (uuid4)
  - [ ] `timestamp` neu setzen
  - [ ] `phase` aus `changes` übernehmen (muss übergeben werden)
  - [ ] alle anderen Felder aus `self` kopieren, Overrides aus `changes` anwenden
  - [ ] gibt neuen frozen `PipelineContext` zurück

---

## models.py — ContextDiff

- [ ] `ContextDiff(BaseModel, frozen=True)`:
  - [ ] `snapshot_a_id: str`
  - [ ] `snapshot_b_id: str`
  - [ ] `added_fields: dict[str, Any]`
  - [ ] `changed_fields: dict[str, tuple[Any, Any]]` — `{field: (alt, neu)}`
  - [ ] `phases_between: list[HookPoint]`

---

## models.py — ContextHistory

- [ ] `ContextHistory` (normales Pydantic-Model, NICHT frozen):
  - [ ] `snapshots: list[PipelineContext]` — chronologisch, intern
  - [ ] `push(ctx: PipelineContext) -> None` — neuen Snapshot anhängen
  - [ ] `current -> PipelineContext` Property — letzter Snapshot (Fehler wenn leer)
  - [ ] `initial -> PipelineContext` Property — erster Snapshot
  - [ ] `at_phase(hook: HookPoint) -> PipelineContext | None` — letzten Snapshot dieser Phase
  - [ ] `between(phase_a: HookPoint, phase_b: HookPoint) -> list[PipelineContext]`
  - [ ] `diff(snap_a: PipelineContext, snap_b: PipelineContext) -> ContextDiff`
    - [ ] Iteriert über alle Felder, erkennt Änderungen und Neufelder
    - [ ] Sammelt alle HookPoints der Snapshots dazwischen
  - [ ] `to_reasoning_trace() -> list[str]` — menschenlesbare Denkgeschichte
    - [ ] Format: `['[ON_INPUT] Input erhalten: "..."', '[ON_MEMORY_HIT] 3 Fakten geladen', ...]`

---

## base.py

- [ ] Import-Block: alle Models + Exceptions aus `core.models` / `core.exceptions`
- [ ] Kurzer Modul-Docstring: Zweck von `base.py` als Einstiegspunkt
- [ ] `TODO`-Kommentar: `BaseHeinzel` kommt in HNZ-002-0002

---

## Tests (test/core/test_models.py)

- [ ] Test: `PipelineContext` lässt sich mit Defaults instanziieren
- [ ] Test: `frozen=True` — direkte Mutation wirft `ValidationError` / `TypeError`
- [ ] Test: `evolve()` erzeugt neuen Snapshot mit geändertem Feld
- [ ] Test: `evolve()` setzt `previous=self` korrekt
- [ ] Test: `evolve()` generiert neue `snapshot_id`
- [ ] Test: `evolve()` setzt neuen `timestamp`
- [ ] Test: `ContextHistory.push()` und `current` / `initial`
- [ ] Test: `ContextHistory.at_phase()` — findet Snapshot, None wenn nicht vorhanden
- [ ] Test: `ContextHistory.between()` — gibt korrekte Teilmenge zurück
- [ ] Test: `ContextHistory.diff()` — erkennt geänderte Felder korrekt
- [ ] Test: `ContextHistory.to_reasoning_trace()` — gibt nicht-leere Liste zurück
- [ ] Test: `HookPoint` — alle 23 Werte vorhanden
- [ ] Test: Keine zirkulären Imports (`import core.models` schlägt nicht fehl)
- [ ] Test: Alle Exceptions importierbar, `__str__` liefert Text

---

## Abschluss

- [ ] `pytest` lokal auf Cirrus grün
- [ ] Kein hardcodierter Pfad, kein Credential im Code
- [ ] `git add src/core/ test/core/ docs/` + einzeiliger Commit
- [ ] Details in `CHANGES.md` eintragen
- [ ] Story-Karte auf Wekan auf **Review** schieben
- [ ] Übergabe-Karte schreiben wenn nächste Session nötig
