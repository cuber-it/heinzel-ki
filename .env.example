# H.E.I.N.Z.E.L. — Umgebungsvariablen
# cp .env.example .env und anpassen
# NIEMALS .env in Git einchecken

# ─── Persistenz ───────────────────────────────────────────────────────────────
# Wo Docker-Daten abgelegt werden (bind mounts)
DOCKER_BASE=$HOME/docker

# ─── PostgreSQL ───────────────────────────────────────────────────────────────
POSTGRES_USER=heinzel
POSTGRES_PASSWORD=changeme
POSTGRES_DB=heinzel

# ─── Mattermost ───────────────────────────────────────────────────────────────
MM_SITEURL=http://localhost:12002

# ─── JupyterHub — langer zufälliger Key: openssl rand -hex 32 ─────────────────
JUPYTERHUB_CRYPT_KEY=changeme

# ─── Gitea ────────────────────────────────────────────────────────────────────
GITEA_ADMIN_USER=heinzel
GITEA_ADMIN_PASSWORD=changeme
GITEA_ADMIN_EMAIL=admin@localhost
