# H.E.I.N.Z.E.L. — Umgebungsvariablen
# cp .env.example .env und anpassen
# NIEMALS .env in Git einchecken

# PostgreSQL
POSTGRES_USER=heinzel
POSTGRES_PASSWORD=changeme
POSTGRES_DB=heinzel

# Mattermost
MM_SITEURL=http://localhost:8001

# JupyterHub — langer zufälliger Key: openssl rand -hex 32
JUPYTERHUB_CRYPT_KEY=changeme
