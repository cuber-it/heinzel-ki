# H.E.I.N.Z.E.L. Umgebungsvariablen
# cp .env.example .env und anpassen
# .env wird NIEMALS in Git eingecheckt

# Pfad zu den persistenten Verzeichnissen (kein trailing slash)
DOCKER_BASE=/home/heinzel-user/docker/heinzel

# PostgreSQL
POSTGRES_USER=heinzel
POSTGRES_PASSWORD=changeme
POSTGRES_DB=heinzel

# Mattermost
MM_SITEURL=http://localhost:8001

# JupyterHub — langer zufälliger Key
# Generieren: openssl rand -hex 32
JUPYTERHUB_CRYPT_KEY=changeme
